// The one constant `envelope.axis` needs, kept on its own so that importing it
// twice - from here and from the script that imports this - is harmless.
decay = 0.2 # sliderBounds: {min: 0, max: 1, step: 0.01}
