// Typechecks the test suites. They run on Node against each package's built
// `dist/`, so they resolve like any other consumer would rather than reaching
// into `src/`.
{
    "compilerOptions": {
        "target": "ES2022",
        "module": "nodenext",
        "moduleResolution": "nodenext",
        "lib": ["ES2022"],
        "types": ["node"],
        "strict": true,
        "esModuleInterop": true,
        "skipLibCheck": true,
        "forceConsistentCasingInFileNames": true,
        "noUnusedLocals": true,
        "noUnusedParameters": true,
        // The suites import their shared helper by its real `.mts` name, which
        // is what Node resolves when it strips the types itself.
        "allowImportingTsExtensions": true,
        "noEmit": true
    },
    "include": ["packages/*/test/**/*"]
}
